{
  "$schema": "https://railway.app/railway.schema.json",
  "services": {
    "api-server": {
      "name": "api-server",
      "source": ".",
      "watch": true,
      "build": {
        "builder": "NIXPACKS"
      },
      "deploy": {
        "startCommand": "pnpm --filter @workspace/api-server build && if [ -n \"$SUPABASE_DB_URL\" ] || [ -n \"$DATABASE_URL\" ]; then pnpm --filter @workspace/db run push:postgres; else pnpm --filter @workspace/db push; fi && pnpm --filter @workspace/api-server start",
        "restartPolicyType": "ON_FAILURE",
        "healthcheckPath": "/api/healthz"
      }
    }
  }
}
